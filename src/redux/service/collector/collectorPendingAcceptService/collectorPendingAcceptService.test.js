/* eslint-disable no-undef */

import {collectorPendingAcceptService} from "./collectorPendingAcceptService";

it("testing", () => {
  const data = {payload: {id: 12}};
  collectorPendingAcceptService(data);
});
