/* eslint-disable no-undef */

import {customerPendingDeclineService} from "./customerPendingDeclineService";

it("testing", () => {
  const data = {payload: {id: 1}};
  customerPendingDeclineService(data);
});
